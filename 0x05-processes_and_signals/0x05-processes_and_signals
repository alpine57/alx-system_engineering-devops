#!/usr/bin/env bash

# Use pgrep to get PIDs of processes
pids=$(pgrep -f bash)

# Iterate over PIDs and get process name using ps
for pid in $pids; do
    process_name=$(ps -o comm= -p "$pid")
    echo "PID: $pid, Process Name: $process_name"
done

